# frozen_string_literal: true

# bundle install (if Gemfile is changed)
# bundle exec jekyll serve (to test server locally) 


source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# gem "rails"

gem "jekyll", "~> 4.2"
gem "webrick", "~> 1.7"

group :jekyll_plugins do
  gem 'jekyll-sitemap'
  gem 'jekyll-feed'
  gem 'jekyll-seo-tag'
  gem 'jekyll-remote-theme'
end

